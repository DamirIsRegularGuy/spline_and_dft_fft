# Проект: DFT, FFT и кубический сплайн

Репозиторий содержит реализацию численного задания по Дискретному Преобразованию Фурье (ДПФ), быстрому преобразованию Фурье (FFT) и кубическому сплайн-интерполированию.

---

## Задача

1. **Дискретное преобразование Фурье (ДПФ)**

   * Вычислить ДПФ для функции
     (f(x) = x(1 - x),\ln(1 - x))
     на равномерной сетке узлов в отрезке ([0,1]) для чисел узлов (N = 10, 50, 100, 1000).
   * Выполнить обратное ДПФ (IDFT) для полученных коэффициентов, оценить погрешность восстановления и построить графики приближения.
   * (Бонус) Реализовать быстрый алгоритм FFT и сравнить по времени/погрешности.

2. **Кубический сплайн-интерполирование**

   * Для функции (f(x) = \sin x) на отрезке ([0,1]) построить кубический сплайн для равномерной сетки узлов при (N = 10, 50, 100, 1000) и показать графики функции и сплайна.

## 🔹 1. Дискретное преобразование Фурье (DFT)

**Задача:**  
Вычислить ДПФ функции  

\[
f(x) = x (1 - x) \ln(1 - x), \quad x \in [0,1]
\]

и восстановить исходный сигнал через обратное ДПФ.  

**Зачем:**  
- ДПФ позволяет переходить из **области времени в частотную область**, что важно для анализа сигналов.  
- Обратное ДПФ показывает точность восстановления функции по спектральным коэффициентам.  

**Что реализовано:**  
- Прямое и обратное ДПФ для N=10, 50, 100, 1000, 10000  
- Вычисление погрешности через норму супремум  
- Сравнение времени выполнения  

**Файл вывода:**  
- `dft_graphics.dat` (анализируемые значения функции, ДПФ и обратное ДПФ)  

**Основная формула ДПФ:**

\[
A_m = \frac{1}{N} \sum_{j=0}^{N-1} f_j e^{-2\pi i m j / N}, \quad
f_j = \sum_{m=0}^{N-1} A_m e^{2\pi i m j / N}
\]

---

## 🔹 2. Быстрое преобразование Фурье (FFT)

**Задача:**  
Реализовать FFT для ускорения вычисления ДПФ.  

**Зачем:**  
- Прямой ДПФ работает за \(O(N^2)\) операций  
- FFT сокращает сложность до \(O(N \log N)\) и используется в реальных приложениях: обработка сигналов, аудио, изображений  

**Что реализовано:**  
- Итеративный алгоритм Cooley-Tukey с битовой перестановкой  
- Прямое и обратное преобразование в одной функции  
- Оценка погрешности  
- Измерение времени выполнения  

**Файл вывода:**  
- `fft_graphics.dat`  

---

## 🔹 3. Кубический сплайн

**Задача:**  
Построить натуральный кубический сплайн для функции  

\[
f(x) = \sin(x), \quad x \in [0,1]
\]

**Зачем:**  
- Кубический сплайн — метод **гладкой интерполяции**, позволяющий аппроксимировать функции более точно, чем простая линейная интерполяция  
- Применяется в графике, моделировании и численных методах  

**Метод:**  
1. Создание равномерной сетки узлов  
2. Вычисление второй производной через метод прогонки (решение трёхдиагональной СЛАУ)  
3. Вычисление коэффициентов кубического сплайна \(a_i, b_i, c_i, d_i\)  
4. Построение гладкого графика на каждом интервале  

**Файл вывода:**  
- `spline_graphics.dat` (значения сплайна и исходной функции sin(x)) 
